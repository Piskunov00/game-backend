{
  "name": "ifrag backend",
  "description": "Python backend for android games",
  "image": "heroku/python",
  "repository": "https://github.com/Piskunov00/game-backend",
  "keywords": ["python", "django" ],
  "addons": [ "heroku-postgresql" ],
  "env": {},
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "python manage.py collectstatic --noinput",
        "test": "python manage.py test"
      }
    }
  }
}
