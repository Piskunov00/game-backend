## GameWithNoName-Server

* Для запуска тестов и локального тестирования
  можно использовать docker-compose

### Например:
```shell script
docker-compose run --rm backend /bin/bash
$ python3 manage.py runserver 0:80
# or
$ pytest -v
```
